import { ScrollArea } from "@/components/ui/scroll-area";

export const Filters = () => {
  return (
    <div className="w-[200px] border-r">
      <ScrollArea className="h-full">Filters</ScrollArea>
    </div>
  );
};
